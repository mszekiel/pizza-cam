import express from 'express';
const router = express.Router();

router.use('/watching', (req, res) => {
    // TO-DO: Display current watching users, time to pizza arrival, something more?
});

export default router;
